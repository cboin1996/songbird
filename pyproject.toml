[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "songbird"
authors = [
    {name = "Christian Boin"},
]
version = "0.0.0"
description = "Music download cli, web interface and general api"
readme = "README.md"
requires-python = ">=3.7"
