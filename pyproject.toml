[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "songbirdcli"
authors = [
    {name = "Christian Boin"},
]
version = "0.1.5"
description = "Songbird's cli."
readme = "README.md"
requires-python = ">=3.9"
