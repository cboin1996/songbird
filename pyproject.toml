[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "songbirdcli"
authors = [
    {name = "Christian Boin"},
]
version = "1.0.0"
description = "Songbird's cli."
readme = "README.md"
requires-python = ">=3.9"
